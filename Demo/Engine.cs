public class Engine
{
    public float Acceleration; // m/s
    public float MaxSpeed; // m/s

    public Engine(float acceleration, float maxSpeed)
    {
        Acceleration = acceleration;
        MaxSpeed = maxSpeed;
    }
}